import React from "react";

const ViewControls = () => {
    return (
        <div className="controls-container">
            this is the download and view controls container
        </div>
    );
};

export default ViewControls;